// 场景二：在画图的空白画布中，用铅笔画方框，约 4 秒后自动结束。
// 先选中铅笔工具；将下面所有坐标改为画布内的物理像素坐标。
// 示例四角：(400,400)、(600,400)、(600,600)、(400,600)。
// 五处 move 包含重复的起点；最后的 move 是框内点 (500,500)。
// 修改后保存、刷新、选择脚本；点击画布，再按 F8。
// 覆盖 move、event、wait 和鼠标 stroke；Down/Up 明确配对。
wait(500);
move(400, 400);
event(Mouse.Left, Down);
wait(400);
move(600, 400);
wait(400);
move(600, 600);
wait(400);
move(400, 600);
wait(400);
move(400, 400);
wait(400);
event(Mouse.Left, Up);

// 释放后移到框内，单击画一个点。
wait(300);
move(500, 500);
stroke(Mouse.Left, 80);
